<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mrabid.pro_maker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MainActivity">
            <action android:name="android.intent.action.MAIN" />
        </activity>
        <activity
            android:name=".Activity.SignUpActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activity.SignInActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".Project" />
        <activity android:name=".Activity.AddTaskActivity" />
        <activity android:name=".Activity.SettingActivity" />
        <activity android:name=".Activity.AddProjectActivity" />
        <activity android:name=".Activity.AddCorporationActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.myCorporationActivity"
            android:label="My Corporation"
            android:theme="@style/Theme.AppCompat">
        </activity>
    </application>

</manifest>